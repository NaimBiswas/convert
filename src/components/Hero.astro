<section class="hero">
  <h1>Free Online Data Converter — JSON, CSV, Excel, XML &amp; YAML</h1>
  <p class="hero-desc">Data Converter is a free online tool that lets developers, analysts, QA engineers, and businesses convert structured data between JSON, XML, CSV, Excel, YAML, and other formats.</p>
  <p class="hero-desc">Unlike many online converters, everything runs directly inside your browser. Your data is never uploaded to our servers, making conversions secure and private.</p>
  <p class="hero-desc">Whether you're debugging APIs, migrating databases, transforming configuration files, or preparing spreadsheets, Data Converter simplifies the process.</p>
  <div class="trust-badge">
    <span class="trust-icon">🔒</span>
    <span>100% client-side processing — your data never reaches any server</span>
  </div>
</section>
<section class="features">
  <h2>Convert Between All Major Data Formats</h2>
  <div class="feature-grid">
    <a href="/json-to-csv-converter" class="feature-card">
      <h3>JSON Converter</h3>
      <p>Convert JSON to CSV, Excel, XML, YAML, HTML, Markdown, or PDF. Handles nested objects, arrays, and large files.</p>
    </a>
    <a href="/csv-to-json-converter" class="feature-card">
      <h3>CSV Converter</h3>
      <p>Convert CSV to JSON, Excel, XML, YAML, and more. Smart header detection and type inference built in.</p>
    </a>
    <a href="/excel-to-json-converter" class="feature-card">
      <h3>Excel Converter</h3>
      <p>Convert Excel (.xlsx) to JSON, CSV, XML, YAML with multi-sheet support. Upload and convert instantly.</p>
    </a>
    <a href="/json-to-excel-converter" class="feature-card">
      <h3>XML &amp; YAML Converter</h3>
      <p>Convert XML or YAML to JSON, CSV, Excel, and other formats. Nested element flattening with underscore separator.</p>
    </a>
  </div>
</section>
<section class="how-it-works">
  <h2>How to Convert Data Online Free</h2>
  <ol class="steps">
    <li><strong>Select formats</strong> — choose source and target format in the converter bar</li>
    <li><strong>Paste or upload</strong> — paste data, upload a file, or fetch from a URL</li>
    <li><strong>Convert instantly</strong> — click Convert or press Ctrl+Enter</li>
    <li><strong>Edit &amp; export</strong> — preview, sort, edit, and download in any format</li>
  </ol>
</section>

<style>
  .hero { text-align: center; padding: 48px 0 24px; }
  .hero h1 {
    font-size: 32px; font-weight: 600; letter-spacing: -0.5px; margin-bottom: 8px;
    transition: color var(--transition);
  }
  .hero-desc {
    color: var(--text-secondary); font-size: 15px; max-width: 600px; margin: 0 auto 8px;
    transition: color var(--transition);
  }
  .hero-desc:last-of-type { margin-bottom: 0; }
  .trust-badge {
    display: inline-flex; align-items: center; gap: 8px;
    margin-top: 16px; padding: 8px 18px;
    background: var(--surface); border: 1px solid var(--border);
    border-radius: 100px; font-size: 13px; font-weight: 450;
    color: var(--text-secondary); transition: all var(--transition);
  }
  .trust-icon { font-size: 15px; }

  .features { padding: 40px 0; text-align: center; }
  .features h2 {
    font-size: 24px; font-weight: 600; letter-spacing: -0.3px;
    margin-bottom: 24px; transition: color var(--transition);
  }
  .feature-grid {
    display: grid; grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
    gap: 16px; max-width: 900px; margin: 0 auto;
  }
  a.feature-card {
    padding: 20px; border: 1px solid var(--border); border-radius: var(--radius);
    background: var(--surface); text-align: left; display: block;
    text-decoration: none; color: inherit;
    transition: all var(--transition);
  }
  a.feature-card:hover { border-color: var(--primary); }
  .feature-card h3 {
    font-size: 16px; font-weight: 550; margin-bottom: 8px; color: var(--text);
  }
  .feature-card p {
    font-size: 13px; line-height: 1.6; color: var(--text-secondary); margin: 0;
  }

  .how-it-works {
    padding: 0 0 40px; max-width: 600px; margin: 0 auto;
  }
  .how-it-works h2 {
    font-size: 20px; font-weight: 600; text-align: center;
    margin-bottom: 20px; letter-spacing: -0.3px;
    transition: color var(--transition);
  }
  .steps {
    list-style: none; padding: 0; counter-reset: step;
  }
  .steps li {
    counter-increment: step; padding: 12px 16px 12px 48px;
    position: relative; font-size: 14px; color: var(--text-secondary);
    border-left: 2px solid var(--border); margin-left: 12px;
    transition: all var(--transition);
  }
  .steps li::before {
    content: counter(step); position: absolute; left: -12px;
    width: 24px; height: 24px; border-radius: 50%;
    background: var(--primary); color: #fff; font-size: 12px; font-weight: 600;
    display: flex; align-items: center; justify-content: center;
  }
  .steps li:last-child { border-left-color: transparent; }
  .steps li strong { color: var(--text); }

  @media (max-width: 640px) {
    .hero h1 { font-size: 24px; }
    .trust-badge { font-size: 12px; padding: 6px 14px; }
    .features h2 { font-size: 20px; }
    .feature-grid { grid-template-columns: 1fr; }
    .how-it-works h2 { font-size: 18px; }
  }
</style>
